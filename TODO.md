# TODO

次回以降の実装候補です。

- ドラッグ配置
- 家具回転
- 2.5Dマップ表示
- 部屋テーマごとのホーム背景演出
- Unity移植用JSONエクスポート拡張

## 補足メモ

- ドラッグ配置は、現在の `room.furniture` と `town.objects[].spotId` を拡張する操作として実装する。
- 家具回転は、スロット/スポット制の次段階で `rotation` をUIから変更できるようにする。
- 2.5Dマップ表示は、スポット制データを表示レイヤーでアイソメトリック風に変換する。
- ホーム背景演出は、`town.theme` や部屋テーマ集計をもとに段階的に切り替える。
- Unity移植用JSONは、家具ID、スポット、回転、町テーマ、所持状態を分けて出力する。
